# Fonts

Lys Finance uses Flutter's platform typography during Sprint 00. This preserves
native rendering, dynamic type, Vietnamese diacritics, and text scaling without
shipping an unnecessary font binary. Any future custom family must include its
license and complete Vietnamese glyph coverage here before being declared in
`pubspec.yaml`.
